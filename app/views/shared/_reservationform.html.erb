<%= simple_form_for [@item, @reservation] do |f| %>
  <%= f.input :start_on, as: :date, order: [:day, :month, :year] %>
  <%= f.input :end_on, as: :date,
                        order: [:day, :month, :year] %>
  <%= f.button :submit, "Reserve now!" %>
<% end %>

